<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.admin.myapplication">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".view.activiti.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.activiti.food.FoodActivity" />
        <activity android:name=".view.activiti.iamge.AlbumActivity" />
        <activity android:name=".view.activiti.food.DetailFoodActivity" />
        <activity android:name=".view.activiti.table.TableActivity" />
        <activity android:name=".view.activiti.material.MaterialActivity" />
        <activity android:name=".view.activiti.report.ReportActivity" />
        <activity android:name=".view.activiti.SettingActivity" />
        <activity android:name=".view.activiti.account.AcountActivity" />
        <activity android:name=".view.activiti.table.DetailTableActivity" />
    </application>

</manifest>